# DrumNext 开发规范

本文档是 DrumNext 的架构说明、开发约束和 AI 协作规范。后续开发者与 AI 在修改代码前应先阅读本文档；若实现与本文档冲突，应先更新文档并说明原因，再修改代码。

## 1. 项目目标

DrumNext 是运行在 Raspberry Pi 5（8 GB）上的空灵鼓投影与远程控制系统。投影端以音游式缩圈、命中高亮、粒子和 Shader 等效果显示乐谱，用户通过手机 App 控制播放、暂停、切歌、速度、布局和高亮。

本项目是完全独立的重构，不是旧项目的渐进升级。

### 1.1 核心目标

- 在 1920×1080、60 Hz 的投影环境中稳定运行。
- 提高视觉效果的开发效率，支持 GPU 加速的 2D 动画、粒子、滤镜和自定义 Shader。
- 手机控制与投影渲染解耦，网络短暂抖动不应直接造成动画卡顿。
- 所有播放结果可由绝对时间计算，暂停、恢复、调速和重连后能够恢复正确状态。
- 代码、配置、资源、测试和部署均可由 AI 通过文本文件完成和验证。

### 1.2 非目标

- 第一阶段不开发新的手机原生 App，只提供稳定的 HTTP/WebSocket 接口和必要的调试页面。
- 第一阶段不使用 3D 引擎。
- 第一阶段不使用 WebGPU，以稳定的 WebGL2 为生产渲染后端。
- 不复用或导入旧项目中的 Python 模块、资源、配置、虚拟环境或构建产物。

## 2. 独立性硬性约束

DrumNext 的项目根目录是本文件所在目录。任何代码都不得依赖父目录中的文件或环境。

必须遵守：

- 禁止从父目录复制运行时数据或通过相对路径读取 `../engine`、`../service`、`../resources` 等内容。
- Python 包、Node 包、静态资源、乐谱、音频、配置和部署脚本必须位于 `DrumNext/` 内。
- Python 使用 DrumNext 自己的 `pyproject.toml` 和 `uv.lock`。
- Node 使用 DrumNext 自己的 `package.json` 和 `package-lock.json`。
- 所有路径都以 DrumNext 项目根目录、应用根目录或显式配置为基准，不依赖进程启动时的当前工作目录。
- 测试不得通过修改 `PYTHONPATH`、软链接或其他方式引用父项目。
- 不在源码中写死开发电脑的绝对路径。
- 构建和测试命令必须能够在只复制 `DrumNext/` 的情况下运行。

如需参考旧项目行为，只能将其视为需求参考，必须在 DrumNext 中重新实现并建立独立测试。

## 3. 技术栈

### 3.1 投影端

- TypeScript：开启严格类型检查。
- PixiJS 8：WebGL2 场景与 GPU 2D 渲染。
- GSAP：待机、页面和非音乐关键动画。
- Vite：开发服务器和生产构建。
- Web Audio API：最终负责采样音频的解码、调度与播放。
- Vitest：纯逻辑、时间轴和协议测试。
- Playwright：浏览器集成测试、截图和视觉回归测试。

投影端不使用 React 或 Vue。PixiJS 直接管理 Canvas 场景；调试 UI 使用少量原生 HTML/CSS。

### 3.2 后端

- Python 3.11 或更高版本。
- uv：依赖和虚拟环境管理。
- FastAPI：HTTP API、WebSocket 和生产静态文件托管。
- Pydantic：请求、响应、配置与事件模型。
- pytest：后端单元和集成测试。

### 3.3 生产环境

- Raspberry Pi OS 64-bit Desktop。
- Chromium kiosk 模式。
- WebGL2 硬件加速。
- systemd 管理后端与 kiosk 启动。

## 4. 总体架构

```text
手机 App / 调试控制页
          │
          │ HTTP REST
          ▼
┌────────────────────────────┐
│ FastAPI 后端               │
│                            │
│ 配置、乐谱、播放会话       │
│ 命令串行化、状态快照       │
│ 静态文件与资源服务         │
└─────────────┬──────────────┘
              │ WebSocket
              │ 快照 + 带时间戳事件
              ▼
┌────────────────────────────┐
│ PixiJS 投影端              │
│                            │
│ 时钟同步、事件调度         │
│ 鼓面场景、动画、粒子       │
│ Shader、音频、性能监控     │
└────────────────────────────┘
```

后端是业务状态的唯一事实来源。投影端维护只读镜像，根据快照和事件进行渲染，不通过投影端反向修改业务状态。

## 5. 预期目录结构

目录按需创建，不应为了占位创建大量空文件。

```text
DrumNext/
├── DEVELOPMENT.md
├── README.md
├── .gitignore
├── package.json
├── package-lock.json
├── pyproject.toml
├── uv.lock
├── shared/
│   ├── schemas/                 # 与语言无关的 JSON Schema
│   └── fixtures/                # 前后端共享的协议测试数据
├── projection/
│   ├── index.html
│   ├── public/
│   ├── src/
│   │   ├── main.ts
│   │   ├── app/
│   │   ├── config/
│   │   ├── network/
│   │   ├── playback/
│   │   ├── scene/
│   │   ├── effects/
│   │   ├── audio/
│   │   ├── shaders/
│   │   ├── debug/
│   │   └── types/
│   └── tests/
├── backend/
│   ├── drumnext/
│   │   ├── main.py
│   │   ├── api/
│   │   ├── domain/
│   │   ├── playback/
│   │   ├── transport/
│   │   ├── storage/
│   │   └── config.py
│   └── tests/
├── resources/
│   ├── scores/
│   ├── audio/
│   ├── images/
│   ├── textures/
│   └── fonts/
├── config/
├── scripts/
└── deploy/
```

根目录统一管理依赖和命令，不在 `projection/` 或 `backend/` 内再建立嵌套的 Git 仓库。

## 6. 领域模型

### 6.1 音位标识

音位使用稳定、与显示文本无关的 `noteKey`，格式暂定为：

```text
low_1 ... low_7
mid_1 ... mid_7
high_1 ... high_7
low_3_center
```

中心音位使用唯一键 `low_3_center`，不通过坐标推断身份。旧乐谱中的该键与旧布局配置的 `low_3` 坐标在布局迁移时显式映射。显示名称、颜色、音频文件和坐标均属于布局配置。

### 6.2 乐谱

内部乐谱使用具名字段，禁止继续使用难以扩展的二维数组作为核心模型：

```json
{
  "schemaVersion": 1,
  "id": "example-score",
  "title": "示例乐谱",
  "durationMs": 120000,
  "notes": [
    {
      "id": "n-000001",
      "timeMs": 3692,
      "noteKey": "low_4",
      "velocity": 0.8
    }
  ]
}
```

约束：

- `timeMs` 是乐谱时间，不是系统时间。
- 音符按 `timeMs` 升序排列。
- 每个音符 ID 在单首乐谱内唯一。
- `velocity` 范围是 0 到 1，缺省为 1。
- 任何格式演进必须增加 `schemaVersion` 并提供迁移或清晰错误。

### 6.3 布局

布局使用归一化坐标，避免与单一投影分辨率绑定：

```json
{
  "schemaVersion": 1,
  "revision": 1,
  "pads": [
    {
      "noteKey": "low_4",
      "x": 0.5,
      "y": 0.5,
      "radius": 0.05,
      "color": "#4A90E2",
      "label": "4",
      "octaveLabel": "L",
      "audioAsset": "low_4.wav"
    }
  ]
}
```

`x`、`y` 和 `radius` 均相对于设计视口；运行时采用等比缩放和居中留边，禁止因屏幕宽高比变化而拉伸鼓面。

## 7. API 与实时协议

### 7.1 REST 职责

REST 用于低频、需要明确成功或失败结果的命令：

- 查询运行状态。
- 播放、暂停和停止。
- 切换乐谱。
- 设置播放速度和播放位置。
- 查询和更新布局。
- 查询可用乐谱与资源。
- 健康检查和版本信息。

API 前缀统一为 `/api/v1`。错误响应应具有稳定的机器可读错误码：

```json
{
  "error": {
    "code": "SCORE_NOT_FOUND",
    "message": "未找到指定乐谱",
    "details": {}
  }
}
```

### 7.2 WebSocket 职责

投影端连接 `/ws/v1/projection`。所有消息必须包含：

```json
{
  "protocolVersion": 1,
  "type": "playback.snapshot",
  "sequence": 1024,
  "serverTimeMs": 18451230,
  "payload": {}
}
```

- `protocolVersion`：协议版本。
- `type`：稳定的事件名称。
- `sequence`：服务端单调递增序号，用于检测丢失、乱序和重连。
- `serverTimeMs`：服务端单调时钟映射值，只用于同一运行会话中的同步。
- `payload`：由消息类型定义的数据。

首批事件类型：

```text
connection.ready
playback.snapshot
playback.started
playback.paused
playback.resumed
playback.stopped
playback.seeked
playback.speed_changed
score.changed
notes.scheduled
layout.changed
ending_animation.changed
highlight.changed
server.error
```

连接建立和重连后，后端必须首先发送完整快照，投影端不能假设自己收到过之前的增量事件。

### 7.3 协议定义

- Python Pydantic 模型和 TypeScript 类型必须表达相同结构。
- 跨边界模型应在 `shared/schemas/` 中具有 JSON Schema。
- 不允许只修改一端协议。
- 每个协议变更至少包含一个共享 fixture，并由前后端测试共同读取。
- 新增字段优先采用向后兼容的可选字段；破坏性修改提升协议版本。

## 8. 播放时间与同步

这是系统中最重要的设计约束。

### 8.1 单一时间公式

播放位置由下式计算：

```text
positionMs = anchorPositionMs + (nowMs - anchorClockMs) × speed
```

暂停时 `positionMs` 固定。恢复、调速或跳转时重新建立锚点，禁止通过逐帧累加 `delta` 作为业务播放位置。

后端使用单调时钟，例如 Python 的 `time.monotonic_ns()`。投影端使用 `performance.now()`。墙上时钟、系统时间和时区不得用于播放调度。

### 8.2 时钟同步

WebSocket 建立后通过多次 ping/pong 估计服务端与浏览器单调时钟偏移：

- 舍弃往返耗时明显异常的样本。
- 使用低延迟样本或中位数估算偏移。
- 平滑修正小偏差，避免画面跳动。
- 偏差超过阈值时通过新快照重建播放锚点。

首版目标：同一局域网中视觉命中误差不超过 30 ms；后续音频调度目标小于 15 ms。

### 8.3 音符预调度

后端应提前发送一个时间窗口内的音符，而不是在命中瞬间发送事件。建议默认：

- 缩圈提前量：1500 ms，可配置。
- 调度前瞻窗口：3000 至 5000 ms。
- 音符事件带唯一 ID，投影端按 ID 去重。

投影端每帧根据绝对播放位置计算效果状态。掉帧后直接显示当前正确状态，不补播已经过时的中间帧。

## 9. 投影端设计规范

### 9.1 设计坐标系

- 设计分辨率固定为 1920×1080。
- Canvas 的 CSS 尺寸填满屏幕。
- 生产环境默认 `resolution: 1`，避免高 DPI 导致像素数量翻倍。
- 使用 contain 方式等比缩放并居中。
- 所有布局计算集中在视口模块，不在效果代码里散落屏幕尺寸判断。

### 9.2 场景层级

从后到前保持稳定层级：

```text
BackgroundLayer
DrumLayer
HighlightLayer
ApproachLayer
HitEffectLayer
OverlayLayer
DebugLayer
```

业务逻辑不得依赖 PixiJS 对象的隐式层级顺序；图层由场景根节点统一创建。

### 9.3 效果接口

视觉效果使用明确生命周期，推荐接口：

```ts
interface VisualEffect {
  readonly id: string;
  update(playbackTimeMs: number): void;
  isComplete(playbackTimeMs: number): boolean;
  destroy(): void;
}
```

要求：

- 效果状态尽量是播放时间的纯函数。
- `destroy()` 必须释放事件监听、纹理引用和 PixiJS 对象。
- 高频对象使用对象池，不能在每帧创建大量临时对象。
- 同类效果共用 Texture、Geometry 和 Filter。
- 禁止为每个粒子创建独立 BlurFilter。
- Filter 范围应明确限制，避免无意中处理整个 1080p 画面。
- Shader uniform 由集中更新器写入，不创建重复 ShaderProgram。

### 9.4 GSAP 使用边界

GSAP 适用于：

- 待机动画。
- 菜单和调试 UI。
- 与乐谱时间无关的转场。

关键缩圈、音符命中和音频不得只依靠 GSAP 回调链推进。它们必须能由绝对播放时间直接求值，以支持暂停、跳转和重连。

### 9.5 资源管理

- 所有资源通过统一 manifest 加载。
- 场景开始前预加载关键纹理、字体和音频。
- 禁止在每个音符出现时从网络重新请求资源。
- 图片优先使用适合显示尺寸的 WebP/PNG，不加载远超实际尺寸的纹理。
- 音频使用本地无损或高质量采样，文件名不作为业务 ID。
- 资源加载失败必须进入可诊断的降级状态，而不是白屏。

## 10. 音频原则

第一阶段允许使用静音或占位采样验证视觉链路。正式音频采用 Web Audio API，并遵循：

- 复用一个 `AudioContext`。
- 启动时解码并缓存鼓声音频。
- 使用 AudioContext 的时间轴提前调度，不使用 `setTimeout` 作为精确音频时钟。
- 同一音位允许重叠播放，不复用仍在播放的单个节点。
- 统一主增益，避免多音叠加削波。
- 页面获得播放授权前展示明确的初始化状态；kiosk 环境应验证自动播放策略。
- 视觉时钟与音频时钟必须有显式映射和测试。

## 11. 后端设计规范

### 11.1 分层

- `domain/`：不依赖 FastAPI 的领域模型和规则。
- `playback/`：播放状态机、时钟与调度。
- `api/`：HTTP 路由和请求/响应转换。
- `transport/`：WebSocket 会话和消息广播。
- `storage/`：乐谱、布局和配置读写。

领域层不得导入 FastAPI。路由函数不直接实现复杂业务逻辑。

### 11.2 状态修改

- 所有写命令串行化处理，避免多个 HTTP 请求同时修改播放对象。
- 外部读取使用不可变快照或深拷贝，不暴露可变内部对象。
- 命令成功后才返回最终状态。
- 重复命令应尽可能幂等，例如显式设置 `paused: true` 优于不确定的 toggle。

### 11.3 文件安全

- 外部输入只能使用资源 ID，不能直接提供任意文件路径。
- 解析后的路径必须验证仍位于允许的资源目录。
- 限制乐谱文件大小、音符数量、字符串长度和请求体大小。
- 所有 JSON 输入由 Pydantic 校验。

## 12. 配置与日志

- 配置来自 `config/` 内的版本化文件和以 `DRUMNEXT_` 开头的环境变量。
- 密钥和设备私有配置不得提交；提供 `.env.example`。
- 不允许业务代码到处直接读取环境变量，统一由配置模块解析。
- 后端日志使用结构化格式，至少包含时间、级别、事件名和上下文。
- 投影端错误应显示简洁的设备状态，同时写入浏览器控制台并尽可能上报后端。
- 日志不得记录认证令牌、完整敏感请求或无上限的大型 payload。

## 13. 性能预算

目标设备为 Raspberry Pi 5 8 GB，生产目标为 1920×1080、60 FPS。

首版预算：

- 正常帧时间低于 16.7 ms。
- 典型场景 P95 帧时间低于 16.7 ms。
- 同屏 100 个缩圈对象时仍保持可用。
- 同屏 500 个轻量粒子时不出现持续掉帧。
- 稳态运行不应持续增长 DOM、Pixi 对象、Texture、Filter 或事件监听数量。
- 断网和 WebSocket 重连期间投影页面不得崩溃或白屏。

性能结论必须在树莓派真机、生产构建和 Chromium kiosk 条件下确认。开发电脑上的 FPS 只能用于发现回退，不能替代真机验收。

调试构建应提供可开关的监控面板：

- 当前 FPS 和帧时间。
- PixiJS 对象及活跃效果数量。
- 粒子数量。
- 播放位置与时钟偏移。
- WebSocket 状态、延迟、最后序号。
- 当前乐谱、速度和调度窗口。

## 14. 测试策略

### 14.1 投影端

必须优先测试纯逻辑：

- 时间锚点公式。
- 暂停、恢复、调速和跳转。
- 音符窗口选择与去重。
- 时钟偏移估算。
- 坐标缩放。
- 协议解析和版本拒绝。
- 效果在给定时间点的确定状态。

Playwright 用于：

- 页面能够启动且 WebGL 初始化成功。
- 使用固定时钟和固定 fixture 截图。
- 断线、重连和完整快照恢复。
- 资源加载失败时显示诊断状态。

视觉测试必须使用确定性随机种子，并关闭与结果无关的动态文本。

### 14.2 后端

- 领域状态机单元测试。
- API 成功和错误响应测试。
- WebSocket 初始快照、序号和重连测试。
- 乐谱与布局校验测试。
- 路径遍历、超限输入和无效协议测试。
- 使用虚拟单调时钟测试播放逻辑，测试中不真实等待数秒。

### 14.3 跨端契约

前后端必须读取相同的 `shared/fixtures/`，验证：

- 合法消息能够解析。
- 缺失必填字段会失败。
- 未支持的协议版本会明确失败。
- 字段命名和单位一致。

所有时间字段名称必须带单位后缀，例如 `timeMs`、`durationMs`。颜色在协议中统一使用 `#RRGGBB` 或 `#RRGGBBAA` 字符串。

## 15. 开发命令约定

实际脚本建立后，根目录应提供以下稳定命令。命令名称一旦被 CI 和文档引用，不应随意改变。

```bash
# 安装依赖
npm install
uv sync

# 开发
npm run dev
uv run drumnext

# 静态检查
npm run typecheck
npm run lint
uv run ruff check .

# 测试
npm test
uv run pytest

# 生产构建
npm run build
```

生产环境由 FastAPI 托管 `projection` 的构建产物。不得使用 Vite 开发服务器作为生产服务器。

## 16. Git 与变更规范

- 一个提交只解决一个明确问题。
- 不提交 `node_modules/`、Python 虚拟环境、浏览器缓存、测试视频或生产构建缓存。
- `package-lock.json` 和 `uv.lock` 必须提交。
- 不静默覆盖用户已有改动。
- 不通过大规模格式化掩盖功能修改。
- 新功能必须同时更新相关测试和文档。
- 协议、配置或资源格式变化必须写明兼容性影响。

## 17. AI 开发工作流

后续 AI 每次执行开发任务时遵循以下流程：

1. 阅读本文档以及任务涉及目录中的局部说明。
2. 检查工作区状态，保留用户未提交的修改。
3. 用一两句话复述目标和关键约束。
4. 先定位现有模型、接口和测试，再修改实现。
5. 优先做最小、完整、可验证的改动，不顺手重构无关模块。
6. 对协议和领域行为先补充或更新测试 fixture。
7. 执行与改动范围对应的类型检查、测试和生产构建。
8. 报告实际验证结果、未验证项和仍存在的风险。

### 17.1 向 AI 描述视觉任务

视觉需求应尽量包含：

- 参考图片、视频或草图。
- 触发条件。
- 起止时间和动画曲线。
- 起止颜色、尺寸、透明度和混合模式。
- 最大并发数量。
- 允许使用的 Filter/Shader。
- 目标分辨率和性能限制。
- 固定时间点的预期截图。

推荐任务示例：

```text
实现 ApproachRing：

- 输入：noteId、位置、颜色、spawnTimeMs、hitTimeMs
- 外半径从 180 px 缩小到 55 px
- 命中后 100 ms 内淡出
- 半径使用 easeOutCubic，透明度在最后 20% 提升
- 同时最多存在 100 个
- 不得每帧创建 Texture、Filter 或 ShaderProgram
- 添加纯时间函数测试和固定时钟截图场景
- 目标为 Raspberry Pi 5 上 1080p/60 FPS
```

### 17.2 AI 禁止事项

- 未验证就声称已修复或性能达标。
- 使用 `setTimeout` 模拟精确乐谱或音频时间轴。
- 将协议字段定义分别手写在多处而不建立契约测试。
- 用 `any`、宽泛异常捕获或关闭类型检查掩盖问题。
- 在渲染循环中进行网络请求、文件读取或大对象分配。
- 为方便而引用 DrumNext 父目录的现成代码或资源。
- 在没有迁移策略时破坏已有乐谱、布局或协议格式。

## 18. 分阶段实施计划

### 阶段 0：项目骨架

- 建立独立 Python 与 Node 依赖。
- 建立格式化、类型检查和测试命令。
- 建立 FastAPI 健康检查。
- 建立 PixiJS 空场景、FPS 面板和 WebGL 能力检测。
- FastAPI 能够托管生产构建产物。

验收：全新目录执行安装、测试和构建成功，代码不引用父目录。

### 阶段 1：视觉原型

- 15 个可配置鼓面。
- 缩圈、命中闪光和基础粒子。
- 固定 fixture 播放。
- 视口等比缩放。
- 真机 1080p 性能基线。

验收：无需后端实时控制即可稳定循环演示，并具有确定性截图测试。

### 阶段 2：播放与通信

- 播放状态机。
- REST 控制接口。
- WebSocket 快照、事件和重连。
- 时钟同步和音符预调度。
- 调试控制页面。

验收：手机浏览器可以控制播放、暂停、调速、切歌，重连后投影恢复正确状态。

### 阶段 3：音频与完整视觉

- 真实空灵鼓采样。
- Web Audio 精确调度。
- 高亮、待机和结束序列。
- Shader 和高级粒子效果。
- 长时间稳定性测试。

验收：音画同步达到目标，连续运行无明显内存增长。

### 阶段 4：设备化部署

- systemd 后端服务。
- Chromium kiosk 自动启动。
- 断电恢复、网络恢复和日志轮转。
- 设备部署、升级与回滚文档。

验收：树莓派冷启动后无需键鼠进入投影界面，后端或浏览器异常后能够自动恢复。

## 19. 架构决策记录

重要决策在 `docs/adr/` 中记录，每个 ADR 至少包含背景、决定、备选方案和后果。以下变化必须新增 ADR：

- 更换核心渲染框架。
- 更换生产通信协议。
- 引入前端 UI 框架或桌面封装。
- 改变音频调度所有权。
- 改变乐谱或布局核心格式。
- 放弃 WebGL2 或改变目标硬件。

本文档描述当前约束；ADR 解释为什么做出重要决定。

## 20. 完成定义

一项任务只有同时满足以下条件才算完成：

- 行为符合任务描述和本文档约束。
- 代码通过相应格式化、静态检查和测试。
- 生产构建成功。
- 新增的公共行为具有测试。
- 协议或配置变化同步更新两端和 fixture。
- 没有引入父目录依赖。
- 没有已知资源泄漏或每帧非必要分配。
- 文档与实际命令、路径和行为一致。
- 向用户明确报告验证范围和遗留风险。
